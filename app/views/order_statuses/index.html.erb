<p style="color: green"><%= notice %></p>

<h1>Order Status</h1>

<p style="color: green"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Name </th>
    </tr>
  </thead>
  <tbody>
  <% @order_statuses.each do |order_status| %>
    <tr>
      <td><%= order_status.name%></td>

      <td><%= link_to 'Show', order_statuses %></td>
      <td><%= link_to 'Edit', order_status(order_status) %></td>
    <tr>
    <% end %>
  </tbody>
</table>

<br>

